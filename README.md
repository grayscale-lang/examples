# Grayscale Examples

Hand-written code examples for [Grayscale](https://github.com/grayscale-lang/grayscale). For full language documentation, see the [Standard](https://github.com/grayscale-lang/grayscale/blob/main/STANDARD.md).

## Examples

- **basics/** — Core language fundamentals
  - `variables/`
  - `arrays/`
  - `maps/`
  - `structs/`
  - `enums/`
  - `functions/`
  - `control-flow/`
  - `pointers/`
  - `error-handling/`
  - `attributes/`
  - `string-interpolation/`
  - `type-aliases/`
- **task-manager/** — CLI task manager with SQLite backend
- **http-server/** — JSON API server
- **thread-pool/** — Concurrent worker pipeline
- **csv-tool/** — CSV query and filter CLI
- **password-manager/** — Password manager CLI with SQLite backend
- **http-client/** — HTTP client CLI
- **benchmarker/** — Benchmark runner with statistics
- **file-watcher/** — File change monitor
- **kv-store/** — Key-value store server
- **encoder/** — Encoding/decoding multi-tool CLI
